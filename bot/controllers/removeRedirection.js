const removeRedirection = async (owner, redirectionId) => {
  // Should be valid redirection


  // User should be the owner of the redirection


  // Remove redirection

  return true;
}

module.exports = removeRedirection;
